import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ padding: '16px', backgroundColor: '#333', color: '#fff' }}>
      <h1>RAWG Games</h1>
    </header>
  );
};

export default Header;